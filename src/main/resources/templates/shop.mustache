<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Магазин</title>
</head>
<body>
    <a href="/account_update">Обновить аккаунт</a>
    <hr>
    <form method="post" action="/user_logout">
        <input type="hidden" name="_csrf" value="{{_csrf.token}}">
        <button type="submit">Выйти</button>
    </form>
    <hr><br><br>

    <h2>Пицца</h2>
    {{#pizza}}
        <p><b>{{name}}</b></p>
        <p><i>{{composition}}</i></p>
        <p>{{cost}} руб</p>
        <img width="128" height="128" src="/images/pizza/{{productID}}.jpeg" alt="{{productID}}">
        <form action="/shop/to_basket" method="get">
            <input type="hidden" value="{{productID}}" name="productID">
            <input type="hidden" name="_csrf" value="{{_csrf.token}}">
            <button type="submit">В корзину</button>
        </form>
        <hr>
    {{/pizza}}
    <h2>Десерты</h2>
    {{#dessert}}
        <p><b>{{name}}</b></p>
        <p><i>{{composition}}</i></p>
        <p>{{cost}} руб</p>
        <img width="128" height="128" src="/images/dessert/{{productID}}.jpeg" alt="{{productID}}">
        <form action="/shop/to_basket" method="get">
            <input type="hidden" value="{{productID}}" name="productID">
            <input type="hidden" name="_csrf" value="{{_csrf.token}}">
            <button type="submit">В корзину</button>
        </form>
        <hr>
    {{/dessert}}
    <h2>Закуски</h2>
    {{#snack}}
        <p><b>{{name}}</b></p>
        <p><i>{{composition}}</i></p>
        <p>{{cost}} руб</p>
        <img width="128" height="128" src="/images/snack/{{productID}}.jpeg" alt="{{productID}}">
        <form action="/shop/to_basket" method="get">
            <input type="hidden" value="{{productID}}" name="productID">
            <input type="hidden" name="_csrf" value="{{_csrf.token}}">
            <button type="submit">В корзину</button>
        </form>
        <hr>
    {{/snack}}
    <h2>Напитки</h2>
    {{#drink}}
        <p><b>{{name}}</b></p>
        <p><i>{{composition}}</i></p>
        <p>{{cost}} руб</p>
        <img width="128" height="128" src="/images/drink/{{productID}}.jpeg" alt="{{productID}}">
        <form action="/shop/to_basket" method="get">
            <input type="hidden" value="{{productID}}" name="productID">
            <input type="hidden" name="_csrf" value="{{_csrf.token}}">
            <button type="submit">В корзину</button>
        </form>
        <hr>
    {{/drink}}
</body>
</html>