<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Магазин</title>
</head>
<body>
    <a href="/account_update">Обновить аккаунт</a><br>
    <a href="/shop/basket">Корзина ({{basketItemsNum}})</a>
    <form method="post" action="/user_logout">
        <input type="hidden" name="_csrf" value="{{_csrf.token}}">
        <button type="submit">Выйти</button>
    </form>
    <hr><br><br>

    <h2>Пицца</h2>
    {{#pizza}}
        <p><b>{{name}}</b></p>
        <p><i>{{composition}}</i></p>
        <p>{{cost}} руб <br> Зависит от диметра пиццы (25 / 30 / 35 см соответственно)</p>
        <img width="128" height="128" src="/images/pizza/{{productID}}.jpeg" alt="{{productID}}">
        <br>
        <form action="/shop/to_basket" method="post">
            <label>
                <select required name="crustType">
                    <option selected disabled>Выберите тесто</option>
                    <option value="thin">Тонкое</option>
                    <option value="thick">Пышное</option>
                </select>
            </label>
            <label>
                <select required name="diameter">
                    <option selected disabled>Выберите диаметр</option>
                    <option value="tf">25 см</option>
                    <option value="th">30 см</option>
                    <option value="thf">35 см</option>
                </select>
            </label>
            <input type="hidden" value="{{productID}}" name="productID">
            <input type="hidden" name="_csrf" value="{{_csrf.token}}">
            <button type="submit">В корзину</button>
        </form>
        <hr>
    {{/pizza}}
    <h2>Десерты</h2>
    {{#dessert}}
        <p><b>{{name}}</b></p>
        <p><i>{{composition}}</i></p>
        <p>{{cost}} руб</p>
        <img width="128" height="128" src="/images/dessert/{{productID}}.jpeg" alt="{{productID}}">
        <form action="/shop/to_basket" method="post">
            <input type="hidden" value="{{productID}}" name="productID">
            <input type="hidden" name="crustType" value="">
            <input type="hidden" name="diameter" value="">
            <input type="hidden" name="_csrf" value="{{_csrf.token}}">
            <button type="submit">В корзину</button>
        </form>
        <hr>
    {{/dessert}}
    <h2>Закуски</h2>
    {{#snack}}
        <p><b>{{name}}</b></p>
        <p><i>{{composition}}</i></p>
        <p>{{cost}} руб</p>
        <img width="128" height="128" src="/images/snack/{{productID}}.jpeg" alt="{{productID}}">
        <form action="/shop/to_basket" method="post">
            <input type="hidden" value="{{productID}}" name="productID">
            <input type="hidden" name="crustType" value="">
            <input type="hidden" name="diameter" value="">
            <input type="hidden" name="_csrf" value="{{_csrf.token}}">
            <button type="submit">В корзину</button>
        </form>
        <hr>
    {{/snack}}
    <h2>Напитки</h2>
    {{#drink}}
        <p><b>{{name}}</b></p>
        <p><i>{{composition}}</i></p>
        <p>{{cost}} руб</p>
        <img width="128" height="128" src="/images/drink/{{productID}}.jpeg" alt="{{productID}}">
        <form action="/shop/to_basket" method="post">
            <input type="hidden" value="{{productID}}" name="productID">
            <input type="hidden" name="crustType" value="">
            <input type="hidden" name="diameter" value="">
            <input type="hidden" name="_csrf" value="{{_csrf.token}}">
            <button type="submit">В корзину</button>
        </form>
        <hr>
    {{/drink}}
</body>
</html>